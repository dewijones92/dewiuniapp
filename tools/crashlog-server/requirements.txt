# Pinned so a rebuild on the Pi is reproducible. All pure-Python wheels, so there is
# no compilation step on arm64 (deliberately avoiding uvicorn[standard], whose uvloop
# and httptools need building).
fastapi==0.118.0
uvicorn==0.37.0
jinja2==3.1.6
